services:
  auto-deploy:
    build: .
    ports:
      - "8004:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /srv/yvideo:/srv/yvideo
      - ./jobs.toml:/app/jobs.toml:ro
    environment:
      DEPLOY_SECRET: ${DEPLOY_SECRET}
    restart: unless-stopped
