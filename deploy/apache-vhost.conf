# Example Apache configuration for yvideo reverse proxy.
# Adapt paths, ServerName, and SSL settings to your environment.
# Required modules: mod_proxy, mod_proxy_http, mod_headers, mod_alias

# ---------- dev ----------
<VirtualHost *:443>
    ServerName dev.yvideo.byu.edu

    # ... your SSL config here (SSLEngine, SSLCertificateFile, etc.) ...

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"

    # Serve static and media files directly from the host filesystem
    Alias /static/ /srv/yvideo/dev/staticfiles/
    Alias /media/  /srv/yvideo/dev/media/

    <Directory /srv/yvideo/dev/staticfiles/>
        Require all granted
    </Directory>
    <Directory /srv/yvideo/dev/media/>
        Require all granted
    </Directory>

    # Do not proxy requests that Apache handles directly
    ProxyPass /static/ !
    ProxyPass /media/  !

    ProxyPass        / http://127.0.0.1:8001/
    ProxyPassReverse / http://127.0.0.1:8001/
</VirtualHost>

# ---------- staging ----------
<VirtualHost *:443>
    ServerName staging.yvideo.byu.edu

    # ... your SSL config here ...

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"

    Alias /static/ /srv/yvideo/staging/staticfiles/
    Alias /media/  /srv/yvideo/staging/media/

    <Directory /srv/yvideo/staging/staticfiles/>
        Require all granted
    </Directory>
    <Directory /srv/yvideo/staging/media/>
        Require all granted
    </Directory>

    ProxyPass /static/ !
    ProxyPass /media/  !

    ProxyPass        / http://127.0.0.1:8002/
    ProxyPassReverse / http://127.0.0.1:8002/
</VirtualHost>

# ---------- prod ----------
<VirtualHost *:443>
    ServerName yvideo.byu.edu

    # ... your SSL config here ...

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"

    Alias /static/ /srv/yvideo/prod/staticfiles/
    Alias /media/  /srv/yvideo/prod/media/

    <Directory /srv/yvideo/prod/staticfiles/>
        Require all granted
    </Directory>
    <Directory /srv/yvideo/prod/media/>
        Require all granted
    </Directory>

    ProxyPass /static/ !
    ProxyPass /media/  !

    ProxyPass        / http://127.0.0.1:8003/
    ProxyPassReverse / http://127.0.0.1:8003/
</VirtualHost>

# ---------- auto-deploy ----------
<VirtualHost *:443>
    ServerName auto-deploy.yvideo.byu.edu

    # ... your SSL config here ...

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"

    ProxyPass        / http://127.0.0.1:8004/
    ProxyPassReverse / http://127.0.0.1:8004/
</VirtualHost>
